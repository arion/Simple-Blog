%h1 Posts Search Result
- @posts.each do |post|
  .post_element
    %h2
      %a{:href => by_category_path(post.category_id)}= h(post.category.title)
      &nbsp;->&nbsp;
      %a{:href => post_path(post)}= post.excerpts.title
    %p= post.excerpts.content
    .tags
      - post.tag_list.each do |tag|
        %a{:href => by_tag_path(tag)}= h(tag)